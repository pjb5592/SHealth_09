# 단계 5 보고서 — TDD 구현 (테스트 보강)

| 항목 | 내용 |
|------|------|
| 워크플로우 | 리팩토링 우선·수정 (`prompt_리펙토링 우선 진행(수정).md`) |
| 단계 | 5 — TDD 구현 (`SHealthBMITest.cpp` Red→Green) |
| 완료일 | 2026-05-20 |
| 산출물 | `src/test/cpp/SHealthBMITest.cpp`, `src/test/fixtures/*.csv`, `SHealth` 최소 결함 수정 |

---

## 수행 작업

1. **`docs/test_plan.md` 기준** `FAIL()` 플레이스홀더 제거 → **35개** Google Test 작성 (Given-When-Then, 단일 조건 per TEST)
2. **P0** — `computeBmi` 5건 (`SHealthTestPeer` + `friend`)
3. **P2** — `classifyBmiCategory` 경계 8건 (18.5 / 23 / 25 포함)
4. **P1** — 체중 0 연령대 보정 픽스처 6건 + `shealth.dat` 스모크
5. **P3** — `isInAgeCohort`, `getBmiRatio`, 빈 CSV, baseline 회귀, idempotent, 실패 시 비율 초기화 등 16건
6. **최소 구현 수정** (`SHealth.cpp` / `.h`)
   - BMI=25 → 비만 분류 (`classifyBmiCategory` 요구사항 체인)
   - `sum==0` / `ageCount==0` 0 나누기 방어
   - `calculateBmi` 시작 시 `cohortRatios_` 초기화
7. **테스트 인프라** — `src/test/fixtures/` CSV 8종, CMake `SHEALTH_FIXTURE_DIR`·`WORKING_DIRECTORY` 설정
8. 워크플로우 5단계 후처리: Report·Prompt transcript·full-transcript 갱신

**유보 (7·6단계)** — `height=0` 보정 TC (TP-P1-07~08), `docs/defect_list.md` §12 DEF-xxx 공식 매핑

---

## 변경 파일

| 경로 | 작업 |
|------|------|
| `src/test/cpp/SHealthBMITest.cpp` | **수정** — 플레이스홀더 제거, 35 TEST/TEST_F |
| `src/test/fixtures/*.csv` | **신규** (8개 픽스처) |
| `src/main/cpp/SHealth.h` | **수정** — `friend class SHealthTestPeer` |
| `src/main/cpp/SHealth.cpp` | **수정** — 분류·보정·집계·초기화 결함 수정 |
| `CMakeLists.txt` | **수정** — fixture 경로 define, ctest `WORKING_DIRECTORY` |
| `Report/05.tdd-implementation-report-2026-05-20.md` | **신규** (본 문서) |
| `Prompt/05.tdd-implementation-transcript-2026-05-20_prompt.md` | **신규** |
| `Prompt/full-transcript-refactor-first-2026-05-20_prompt.md` | **갱신** |

---

## 검증 결과

| 검증 항목 | 결과 | 비고 |
|-----------|------|------|
| `cmake --build build` | **통과** | `SHealthBMI`, `SHealthBMITest`, `shealth_lib` 빌드 성공 |
| `ctest` | **통과** | **35/35** Green (`100% tests passed`) |
| `SHealthBMI` 수동 출력 | **통과** | `docs/refactor_baseline_output.txt`와 **6행 일치** |
| Baseline 회귀 TC | **통과** | `GetBmiRatio_ShealthDat_MatchesBaseline` (ε=1e-3) |

### 테스트 구성 요약

| 영역 | 우선순위 | TC 수 | 비고 |
|------|----------|-------|------|
| BMI 계산 | P0 | 5 | `EXPECT_NEAR` ε=1e-5 |
| 4단계 분류 | P2 | 8 | BMI=25 비만 (DEF 회귀) |
| 연령대 보정 | P1 | 6 | weight=0; height=0 유보 |
| 통계·API | P3 | 16 | baseline·sum=0·잘못된 인자 |

### 결함 수정 요약 (test_plan §6 반영)

| ID | 수정 내용 | 대표 TC |
|----|-----------|---------|
| EX-04 | BMI=25 → 비만 | `ClassifyBmi_25_ReturnsObesity`, `GetBmiRatio_Bmi25CountedAsObesity` |
| EX-01 | sum=0 → 0% | `GetBmiRatio_EmptyCohort_ReturnsZeroNoDivideByZero` |
| EX-02 | 전원 weight=0 보정 스킵 | `ImputeWeight_AllZeroInCohort_LeavesZeroNoCrash` |
| EX-06 | 실패 시 비율 초기화 | `CalculateBmi_FailedLoad_ClearsPreviousRatios` |

---

## 다음 단계 권고

**9단계 — Golden Master (선택, 수정 워크플로우 #9)**

```
@docs/refactor_baseline_output.txt
@src/main/cpp/SHealthBMI.cpp
```

- `SHealthBMI` stdout 전체 diff 자동화 또는 9단계 전용 Golden TC

**미완 워크플로우 (수정 순서)**

| 단계 | 산출물 | 비고 |
|------|--------|------|
| 6 | `docs/defect_list.md` | §12 DEF-xxx ↔ TC 매핑 공식화 |
| 7 | Activities 4 기능 | height=0 보정, F-04/F-05 API |
| 8 | `docs/refactoring_plan.md` | 2차 리팩토링 로드맵 |

**7단계 후** — `height=0` TC 추가·`GTEST_SKIP` 해제, baseline 갱신 여부 PR 기록

완료 후 동일 후처리: `Report/09.golden-master-report-2026-05-20.md` 등
