# 단계 0 보고서 — `.cursorrules` 작성

| 항목 | 내용 |
|------|------|
| 워크플로우 | 리팩토링 우선 (`prompt_리펙토링 우선 진행.md`) |
| 단계 | 0 — 프로젝트 규칙 (Cursor Rules) |
| 완료일 | 2026-05-20 |
| 산출물 | `.cursorrules` |

---

## 수행 작업

1. `prompt_리펙토링 우선 진행.md` 0단계 프롬프트 및 공통 컨텍스트를 반영하여 프로젝트 루트 `.cursorrules` 작성
2. 기술 스택(C++17, CMake, Google Test, `shealth_lib`) 및 주요 소스 경로 정의
3. 도메인 규칙 정리: BMI 공식, 4단계 분류(18.5/23/25), 연령대 `[a,a+10)`, 체중 0 평균 보정, `getBmiRatio` API
4. 리팩토링 우선 워크플로우 0~12단계 표·검증 기준(0~6: 빌드·수동 / 8~: ctest Green) 명시
5. README Activities 1~5를 리팩토링 우선 관점으로 재매핑 (테스트는 7~8단계 후반)
6. 리팩토링·테스트 원칙, AI 작업 체크리스트, 빠른 참조 지시문 추가

---

## 변경 파일

| 경로 | 작업 |
|------|------|
| `.cursorrules` | **신규** (약 197행, 한글 설명) |

**코드·빌드 산출물 변경 없음** (`src/`, `CMakeLists.txt`, `docs/` 미변경)

---

## 검증 결과

| 검증 항목 | 결과 | 비고 |
|-----------|------|------|
| `cmake --build build` | **미실행** | 0단계는 규칙 파일만 생성 — 빌드 필수 아님 |
| `ctest` | **미실행** | 8단계 이후 완료 조건 |
| `SHealthBMI` 수동 출력 | **미실행** | 코드 변경 없음 |
| 산출물 존재 | **통과** | `.cursorrules` 프로젝트 루트에 생성 확인 |

---

## 다음 단계 권고

**1단계 — 요구사항 분석**

```
@README.md @bmi.png
→ docs/requirements_analysis.md
```

- README·BMI 도메인을 C++ 구현·리팩토링·(후반) 테스트 관점으로 표·번호 목록으로 정리
- `getBmiRatio` API 계약, Activities 4 기능 개선 항목, 8단계용 Google Test 시나리오 30건 이상 목록 포함
- **코드 변경 없음** — 문서만 생성

완료 후 동일 후처리: `Report/1.requirements-analysis-report-2026-05-20.md` 등
